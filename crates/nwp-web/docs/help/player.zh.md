# 使用说明

## 跟读练习

本页把音频和歌词放在一起，方便跟读。可以打开本地文件夹、选择文件，或加载内置音频书（例如 Byen Er Bergen）。

* 音频与歌词按文件名配对。同一时间戳的两行会成为原文与译文。
* 点击歌词行可跳转到对应位置。底部按钮可换行、调速，或循环当前句。
* 内置音频书来自 `data/audio`。也可以把音频和 `.lrc` / `.srt` 拖进页面。

用「返回上一级页面」回到练习模式选择页。
